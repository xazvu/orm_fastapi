# from sqlalchemy.orm import Session
# from fastapi import Depends
# from db.engine import get_db
# from db.models import User
#
# def get_user(
#         session: Session = Depends(get_db)
# ):
#     activity = session.query(User).all()
#
